version: 1
packages:
  - name: "postgres"
    path: "gateways/postgres"
    queries: "gateways/postgres/sqlc_queries"
    schema: "gateways/postgres/migration"
    engine: "postgresql"
    sql_package: "pgx/v4"
    emit_db_tags: true
    emit_empty_slices: true
